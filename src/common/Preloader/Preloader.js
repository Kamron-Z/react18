import loading from '../../assest/images/loading.gif'

const Preloader = (props) => {
    return (
        <div>

            <img src={loading} alt=""/>
        </div>
    )
}

export default Preloader